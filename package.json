{
  "name": "saas-monorepo",
  "private": true,
  "workspaces": [
    "frontend/*",
    "backend/*",
    "shared/*"
  ],
  "scripts": {
    "start": "lerna run start",
    "build": "lerna run build",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^5.0.0",
    "typescript": "^4.5.0"
  }
}
